apiVersion: apps/v1
kind: Deployment
metadata:
  name: strike-server2
  namespace: strike
  labels:
    app: strike-server2
spec:
  selector:
    matchLabels:
      app: strike-server2
  template:
    metadata:
      labels:
        app: strike-server2
    spec:
      containers:
      - name: strike-server2
        image: strike_server
        ports:
        - containerPort: 8081
        envFrom:
            - secretRef:
                name: strike-server2-env
        volumeMounts:
            - name: strike-server2-identity
              mountPath: /etc/strike-server2-identity
              readOnly: true
            - name: strike-federation
              mountPath: /etc/strike-federation
      volumes:
        - name: strike-server2-identity
          secret:
            secretName: strike-server2-identity
        - name: strike-federation
          secret:
            secretName: strike-federation
