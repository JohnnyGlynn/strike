apiVersion: apps/v1
kind: Deployment
metadata:
  name: strike1-db
  namespace: strike
  labels:
    app: strike1-db
spec:
  selector:
    matchLabels:
      app: strike1-db
  template:
    metadata:
      labels:
        app: strike1-db
    spec:
      containers:
      - name: strike1-db
        image: strike-db 
        ports:
        - containerPort: 5432
        envFrom:
            - secretRef:
                name: strike-db1-env
