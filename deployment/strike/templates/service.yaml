apiVersion: v1
kind: Service
metadata:
  name: {{ include "strike.fullname" . }}
  labels:
    {{- include "strike.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "strike.selectorLabels" . | nindent 4 }}
